# Stripe Configuration
# Get your keys from https://dashboard.stripe.com/apikeys

# Client-side publishable key (starts with pk_test_ or pk_live_)
# Required for Stripe Checkout to work
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Price ID for the $0.01 product
# Create a product + price in your Stripe Dashboard (https://dashboard.stripe.com/products)
# then copy the Price ID (starts with price_)
VITE_STRIPE_PRICE_ID=price_your_price_id_here

# --- Optional: Only needed if running the Express backend server (server/server.js) ---
# Server-side secret key (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Frontend URL (used for Stripe Checkout redirect URLs in server mode)
CLIENT_URL=http://localhost:5173
